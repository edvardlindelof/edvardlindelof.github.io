# Edvard's blog

## Setup
[Get RubyGems](https://jekyllrb.com/docs/installation/ubuntu/), then install jekyll:
```bash
gem install jekyll bundler
```

## Build and serve locally
```bash
bundle exec jekyll serve
```
